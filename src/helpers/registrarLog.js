// src/helpers/registrarLog.js

// Dummy: no hace nada, después se reemplaza por la versión funcional
export default function registrarLog(info) {
  // console.log("Log registrado (dummy)", info);
}
